# toksight Design Specification

> v7（2026-09）：从零重做。`toksight web` 不再是多格仪表盘 + 配置页，而是一页**按月 / 按年的
> token 用量与成本报告**：稀疏点阵底 + Claude 明暗配色，两张可拖动排序的章节卡片，整页可导出
> 为一张分享图。本文件是网页视觉与交互的唯一依据；实现位于 `web/`，颜色全部是
> `web/app/globals.css` 里的 CSS 变量。
>
> v7.1：点击热力图上的某一天，报告列旁展开一张**单日详情卡片**（宽屏与报告列并排居中、窄屏
> 悬浮），主报告不变。
>
> v7.2：模型分布并入 Agent 卡片。每个 Agent 下列出其模型费用；章节卡片变为两张
> （热力图 · Agent 与模型）。
>
> v7.3：Agent 行改为可展开的目录，模型默认折叠，点击 Agent 行才展开其模型费用。
>
> v8：Tokens 与费用合并展示，去掉全部 Tokens/费用 切换。Agent 卡片从排行长条改为可展开的
> 表格（点击表头按 Tokens 或费用排序）；加入 Agent 身份色、构成色条、缓存命中环与 KPI 彩色
> 图标；单价、会话数、原始模型 ID 等次要参数移入行悬停提示。

## 1. 方向

- **产品**：本地优先的 AI coding agent 用量报告。只读取 Agent 文件，刷新时写入本项目的 SQLite
  用量数据库。只统计 token 与成本，不展示或修改任何
  agent 配置。Cursor 用量 CSV 可经网页导入 toksight 自己的 SQLite；数据不出机器。
- **气质**：像 Claude 的界面一样安静、温暖、留白充足——暖白/暖灰底、衬线标题、柔和圆角与
  轻阴影、橙色强调。颜色多但各有固定含义（见第 2 节），数据清楚，装饰克制。
- **硬约束**：浅色 + 深色两套（可跟随系统）；中英可切；根 CLI 零运行时依赖（网页依赖只在
  `web/`，构建期打包）；原生 CSS（无 Tailwind / 组件库）；字体全部本地打包，运行时不联网。

## 2. 颜色

| 变量 | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| `--bg` | `#f5f4ed` | `#262624` | 页面底 |
| `--dot` | `rgba(20,20,19,.15)` | `rgba(250,249,245,.11)` | 点阵 |
| `--surface` | `#ffffff` | `#30302e` | 卡片、KPI 条 |
| `--surface-2` | `#f3f1ea` | `#3a3a37` | 统计小块、进度槽、分段控件底 |
| `--text` / `-2` / `-3` | `#141413` / `#3d3d3a` / `#75746e` | `#faf9f5` / `#c9c6bc` / `#9c9a92` | 正文 / 次要 / 说明 |
| `--accent` | `#d97757` | `#d97757` | 强调：费用条、焦点环 |
| `--btn` | `#c6613f` | `#d97757` | 主按钮（导出图片） |

- **热力图 5 档**（Claude 橙强度）：浅 `#efede6 #f6dccf #eeb89e #e08e6c #c15f3c`，深
  `#3a3a37 #5b3e31 #86503b #b3613f #d97757`。第 3、4 档上的文字用白色。深浅只按 tokens。
- **Token 构成 4 色**（Anthropic 插画色系）：输入 sky `#6a9bcc`、缓存读 olive `#788c5d`、
  缓存写 kraft `#d4a27f`、输出 clay `#d97757`（深色主题各自提亮一档）。用于构成色条、时段柱、
  提示里的色块与图例；缓存命中环用缓存读的 olive。
- **Agent 身份色**（`--agent-<id>`，每个 Agent 在所有周期、卡片与导出图里固定一色）：
  claude `#d97757`、codex `#3d8c7e`、cursor `#5a73c4`、opencode `#b95c8c`、kimi `#c19a3c`、
  zcode `#8a6bbf`，未知 Agent `#8a8780`；深色主题各自提亮。用于 Agent 圆点、模型数小标签底色、
  展开后模型行的竖线与淡底色、会话左侧竖线。
- **KPI 图标色**：Tokens sky、参考费用 `--accent`、缓存命中 olive、请求 `--c-requests`
  （`#8a6bbf`）；图标底为同色 15% 透明。

## 3. 字体

- **衬线**：Source Serif 4（`@fontsource-variable/source-serif-4`，`next/font/local` 自托管拉丁子集）
  用于品牌名、卡片标题、KPI 与统计数值。中文字形回退到苹方 / 微软雅黑。
- **无衬线**：Geist Sans，正文、标签、按钮、表格数值（`tabular-nums`）。
- **等宽**：Geist Mono，仅模型名与代码片段。
- 字号：KPI 30、卡片标题 23、统计值 20、正文 14–15、说明 12–13。

## 4. 布局

1. **点阵底**：`body` 背景 `radial-gradient` 圆点，间距 28px，只落在网格交点上。
2. **顶栏**（粘性，≤760px 取消粘性）：左品牌（3×3 热力小方格 + 衬线 `toksight`）；右依次为
   月/年分段、‹ 周期 ›、配色分段（浅/深/系统图标）、中/EN 分段、刷新图标按钮。
3. **报告列** `.report`（最大宽 1000px 居中，是导出图片的截取范围）：
   - **页首**：直接显示 4 格 KPI 条（Tokens、参考费用、缓存命中率、请求），格间 1px 细线；
     每格标签前是 22px 的彩色图标块。Tokens 格只显示总量及输入 / 输出文字；参考费用格的
     说明行在全部定价时显示“每百万 tokens $X”（混合单价），有未定价模型时显示其数量。
     报告标题、日期范围与 agent / 模型数不在页面上显示，周期只在顶栏标示。
   - **两张章节卡片**（纵向，间距 22px，圆角 20px，1px 边 + 轻阴影）。卡头：衬线标题 + 说明行
     （周期 · 着色或排序依据）；右侧只有拖动手柄，没有指标切换。
   - **页脚**：版本、生成时间、时区、估算声明、本地声明；未定价模型单独一行。
4. **报告操作**：在报告列左侧紧贴首个 KPI 条放 3 张纵向小卡片，依次为“导入 Cursor CSV”、
   橙色“导出图片”、“更新单价”；它们在 `.report` 外，不进入导出图。侧边空间不足或单日卡片
   展开时，小卡片在报告上方排为一行；手机宽度下图标与文字上下排列。
5. **卡片内容与尺寸**（每张卡高度有上限，方便导出）：
   - 活动热力图：4 个统计小块（活跃天数；活跃日均与峰值日以 tokens 为主值、费用写在说明行；
     最长连续）→ 月视图为 7 列日历（周一开头，格高 66px，左上日期，左下 tokens 加粗、其下一行
     费用；≤760px 只留 tokens，≤560px 隐藏数值）；年视图为 53 周 × 7 天小方格
     （圆角 3px，月份标签在上，一/三/五 在左，窄屏横向滚动）→ 右下“少 □□□□□ 多”图例。
     未来日期透明描边，今天加描边环（橙），选中日加双层环（`--surface` 间隔 + `--text`），
     悬停过去日期时出现细描边。图例左侧是“点击日期查看当天详情”提示（`.no-export`）。
   - Agent 与模型：一张表（`table-layout: fixed`，表头下 1px 线，Agent 行之间 1px 线），列为
     Agent · Tokens · 费用 · Token 构成 · 缓存命中 · 请求。Agent 格：› 箭头 · 10px 身份色圆点 ·
     名称 · “N 个模型”小标签（身份色 14% 底）。Tokens 与费用格都是“加粗数值 + 灰色占本期份额”；
     未定价写“未定价”，部分未定价在金额后加黄色 `*`，图例行说明。构成格是 96px、6px 高的
     100% 堆叠色条；缓存命中格是 14px olive 圆环 + 百分比。没有名次、长条和说明行。
     模型默认折叠；展开后箭头转为 ⌄，模型行铺上该 Agent 身份色 5% 的底色，左侧有一条从箭头
     引出的 2px 身份色竖线，等宽模型名与 Agent 名称对齐，列与 Agent 行相同；模型与 Agent 用同一
     排序。单个 Agent 超过 8 个模型时，第 8 行为“其他 N 项模型用量”。卡片底部为构成图例、
     `*` 说明与“点击 Agent 展开模型 · 悬停查看明细”提示（`.no-export`）。
     表格按自身宽度（容器查询）逐级收列：≤640px 去掉构成列与图例色块，≤540px 去掉请求，
     ≤460px 去掉份额与模型数标签，≤400px 去掉缓存命中。
6. **单日详情卡片** `.day-panel`（在 `.report` 之外，永不进入导出图）：与章节卡片同一套外观——
   `--surface` 底、1px 边、圆角 20px；宽 `clamp(360px, 28vw, 420px)`。
   - **并排 / 悬浮**：视口 ≥ 1180px 时，`.workspace` 里报告列右侧的 `.day-dock` 展开出卡片宽度，
     报告列与卡片作为一组重新居中（空间不够时报告列收窄）；卡片与章节卡片间距 24px，右侧留白与
     报告列左侧对称（44px），轻阴影 `--shadow`，`position: sticky` 从报告顶部（78px）起吸附，
     高度为视口剩余高度。顶栏宽度与排版保持不变，不随卡片开合重排。更窄时卡片悬浮在页面右侧（四周留
     12px，`--shadow-lift`）；≤ 760px 占满宽度（四周 8px）。
   - **头部**（固定不滚动）：橙色小标题“单日详情” + ‹ › × 图标按钮 → 衬线日期标题（32px）→
     年 · 星期 · 今天。
   - **正文**（独立滚动）：2×2 统计块（与页首 KPI 相同的内容与图标，`--surface-2` 底）→
     时段分布（24 根本地小时柱，按 tokens 线性缩放到最忙的小时，按构成 4 色自下而上分段；
     轴标 00/06/12/18/24:00；悬停提示含 tokens 与费用）→ Agent 与模型（与卡片相同的表格，
     排序与主报告共用，展开状态互不影响，字号小一档；卡片宽度下通常只剩 Agent · Tokens · 费用）→
     会话（服务器按 tokens 排出的前 10 个，左侧 3px 身份色竖线：标题或目录名 · tokens · 费用 →
     agent · 起止时间 · 活跃时长 · 请求 → 等宽的目录与模型）。
     分区之间 1px 细线，分区标题衬线 18px，右侧说明 12px。

## 5. 交互

- **周期**：月 / 年两种；向前最多到 `scopeRange.firstAt` 所在周期，向后不超过今天。切换到月
  模式时若落在未来，回到本月。加载新周期时旧内容半透明保留，数据到达后整体替换。
- **拖动排序**：只能从手柄拖（Pointer Events，鼠标 / 触摸通用）。拖起的卡片跟随指针并加深阴影，
  其余卡片 0.22s 让位；靠近视口上下 80px 自动滚动；松手 FLIP 归位 0.24s。手柄聚焦时 ↑ / ↓
  移动一格，移动后焦点留在手柄上。顺序存 `localStorage`。
- **展开 Agent**：Agent 名称格是一个按钮（`aria-expanded` 指向该 Agent 的模型 `<tbody>`，
  Enter / 空格可操作），点击整行任意位置都会在折叠与展开之间切换，可同时展开多个。悬停时整行
  浅底、箭头变为 `--accent-strong`。展开状态只存在当前页面，翻页或改排序时保留，刷新页面后
  全部折叠；没有模型的 Agent 不显示箭头。
- **排序**：Tokens 与费用两个表头是按钮，点击后 Agent 与其模型都按该列降序（同值按 tokens）；
  当前列表头变深并带橙色 ↓（`aria-sort="descending"`），其他列悬停时淡出 ↓。卡片说明行同步
  写“按 Tokens 排序 / 按费用排序”。主报告与单日卡片共用这个排序。
- **行悬停提示**：Agent、模型与“其他”行悬停时显示四类 token 的绝对数量（带构成色块）、缓存
  命中、请求 / 会话；模型行在各原始 ID 费率一致时于右列并排给出四类每百万 tokens 单价与来源，
  合并了多个原始 ID 时列出这些 ID。窄表收起的列也能在这里看到。
- **单日详情**：点击热力图上今天及以前的任意一天（月视图的日期格是按钮，可键盘操作；年视图
  小方格可点击）打开详情卡片；再次点击同一天、按 Esc 或点 × 关闭。卡片不是模态：打开时仍可在热力图
  上点别的日子切换，主报告照常翻页、拖动、导出。‹ › 逐日前后移动，最早到 `scopeRange.firstAt`
  所在日，最晚到今天，不带动主报告的周期。数据单独请求 `period=custom&since=<日>&until=<日>`；
  切换日期时旧内容半透明保留。打开时焦点移入卡片，关闭后回到打开前的日期格。刷新按钮同时刷新
  卡片。选中环在导出时去掉（导出期间 `.report` 带 `.is-exporting`）。
- **导出图片**：`modern-screenshot` 把 `.report` 渲染为 PNG（至少 2 倍像素），克隆时去掉所有
  `.no-export` 元素（拖动手柄、Agent 展开箭头、点击提示），把点阵画到图片底上；排序表头的
  ↓ 保留，说明当前顺序。Agent 的展开状态按屏幕原样导出：展开的 Agent 带出其模型，折叠的只有
  Agent 行。文件名 `toksight-<YYYY-MM|YYYY>.png`。
- **Cursor CSV 导入**：报告操作区选择本地 Usage Events CSV，导入成功后报告跳转到该文件最后一条记录
  所在的月或年；重复行去重，零 token 行跳过。导入提示在报告外，不进入导出图。
- **更新单价**：报告操作区按钮强制检查 LiteLLM 和 Cursor 两个公开来源；更新后重载当前报告与单日卡片，
  页脚显示各来源上次成功拉取时间。同一模型行的各原始 ID 费率一致时，其悬停提示显示四类 token 单价。
- **偏好**（均存 `localStorage`，读取时校验）：`toksight-locale`、`toksight-theme`
  （light/dark/system）、`toksight-period-mode`、`toksight-card-order`、`toksight-agent-sort`
  （tokens/cost）。选中的日期与 Agent 展开状态不持久化；旧版的 `toksight-card-metrics`、
  `toksight-day-metric` 不再读取。
  `<head>` 内联脚本在首帧前写入 `data-theme`，避免闪烁。
- **动效**：只有分段/按钮/表头 0.15s 颜色过渡、排序箭头 0.15s 淡入、Agent 箭头 0.15s 旋转（模型行直接出现，不做高度动画）、弹出提示 0.22s 上浮淡入 / 0.2s 淡出、拖动让位与归位、详情卡片 0.36s 展开收起（并排时
  报告列平移让位、顶栏不动，卡片从报告列边缘淡入并右移 16px 就位，被 `.day-dock` 裁切成
  “从列边抽出”；悬浮时从右侧 24px 淡入并由 0.98 放大）、加载骨架脉冲、刷新图标旋转；
  `prefers-reduced-motion` 下全部关闭。不做入场
  编排、数字滚动。

## 6. 状态

- 加载：KPI 条（134px）与两张卡片形状的骨架块。
- 无任何记录：单卡片 + Inbox 图标 + `toksight env` 提示；导出按钮禁用。
- 本期无记录：卡片内一行“这个周期没有记录。”，热力图照常画空格子。
- 请求失败：无数据时为错误卡片 + 重试；已有数据时弹出红色提示，保留旧内容。
- 采集警告：弹出黄色提示，可展开明细；同一组警告只弹一次，内容变化后再弹。
- 弹出提示（toast）：视口底部居中堆叠（最多 4 条，宽 ≤ 460px），`--surface` 底 + `--shadow-lift`，
  图标与边框按 成功（橙）/ 警告（黄）/ 错误（红）着色；成功 4 秒、警告与错误 8 秒后自动淡出，
  悬停、聚焦或展开明细时暂停，× 立即关闭。刷新、更新单价、导入 CSV、导出图片的结果与失败都走
  弹出提示；同类操作的新提示替换旧的。位于 `.report` 之外，不进入导出图。
- 详情卡片：首次加载为统计块与分区形状的骨架；当天无记录时正文一行“这一天没有记录。”；请求失败时
  正文顶部红色提示条 + 重试，已有内容半透明保留。

## 7. 禁止事项

- 不加回配置页或任何写入操作。
- 不用彩虹色板，也不加回 Tokens/费用 切换。每种颜色只有一种含义：Claude 橙是强调与热力，
  构成 4 色只表示 token 类别，Agent 身份色只标识 Agent，KPI 图标色只用于 KPI 图标。
- 排名不用长条表达；Agent 与模型用表格数值和份额。
- 不使用 Google Fonts `@import` 或任何运行时字体请求。
- 不用 UTC 日期；日界与周期都按本机本地时区。
- 不把未定价模型藏起来——页脚必须列出。
- 中文界面不夹未翻译的界面文案。
